export interface AnimalModel {
    id: string
    ong: string
    name: string
    type: string
    breed: string
    height: number
    weight: number
    special_needs: string[]
    adoption_requirements: string[]
    photo: string
    adopter: string
}